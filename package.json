{
  "name": "c4",
  "version": "1.0.0",
  "private": true,
  "description": "ACM UGA Connect Four hackathon tournament platform",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "dev": "pnpm -r --parallel dev",
    "fixtures": "pnpm -C packages/samples fixtures",
    "check:workers": "node scripts/check-workers.mjs",
    "deploy:workers": "pnpm -C apps/practice-bots exec wrangler deploy && pnpm -C apps/testground build && pnpm -C apps/testground exec wrangler deploy && pnpm run check:workers"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@12.4.2"
}

{
  "name": "@connect-4/generate-fixtures",
  "version": "1.0.0",
  "description": "Script that synthesizes a full plausible tournament for presenter rehearsal",
  "type": "module",
  "main": "./dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "fixtures": "tsc -p tsconfig.json && node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@connect-4/contract": "workspace:*",
    "@connect-4/engine": "workspace:*",
    "@connect-4/match-engine": "workspace:*",
    "@connect-4/practice-bots": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

"""Never actually built: roster.ts rewrites this team's repo_url to a path
that doesn't exist, so the engine's checkout step fails first. This file
exists only so materialize/overlay-rule checks have something to validate."""


def choose_move(board, you, info=None):
    return 0

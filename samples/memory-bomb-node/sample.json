{
  "name": "Memory Bomb Node",
  "template": "node",
  "members": [
    "Sample Bot"
  ],
  "expect": {
    "kind": "game_forfeit",
    "reason": "crash_loop|clock_expired",
    "notes": "tier-3 resource abuse: allocates until OOM-killed. Build-only until the hardened engine (512MB cap, no swap) lands; never run outside it. OOM restarts cost clock time, so the forfeit is crash_loop or clock_expired depending on timing (both observed on the arena)."
  },
  "requiresHardenedEngine": true,
  "strength": 0
}
